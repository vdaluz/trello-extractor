source 'https://rubygems.org'

ruby '>= 2.7.0'

# No external gems needed for basic functionality
# JSON parsing is built into Ruby
# HTTP client (Net::HTTP) is built into Ruby
# File operations (FileUtils) are built into Ruby

group :development do
  gem 'rubocop', '~> 1.0'  # Code style checker (optional)
end 